<%= form_with(model: user_asset, local: true) do |form| %>
  <% if user_asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_asset.errors.count, "error") %> prohibited this user_asset from being saved:</h2>

      <ul>
      <% user_asset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :asset_name %>
    <%= form.text_field :asset_name %>
  </div>

  <div class="field">
    <%= form.label :count %>
    <%= form.number_field :count %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.number_field :status %>
  </div>

  <div class="field">
    <%= form.label :returned %>
    <%= form.number_field :returned %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
